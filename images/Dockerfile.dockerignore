# Applies to the build context set in docker-compose.yml (context: .., i.e. the
# repository root). The file name follows the Docker convention
# "<Dockerfile>.dockerignore" in the context root, which is what lets the
# .dockerignore live inside images/ rather than at the repository root.
.git
.gitignore
.idea
venv
.venv
__pycache__
*.pyc
*.pyo
*.log
log
parser_data
design_number_data
tests
.pytest_cache
pytest.ini
requirements-dev.txt
.env
.env.example
images
*.docx
